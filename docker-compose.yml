Version: '3.8'

services:
  poke_service:
    image: poke_service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./artifacts:/app/artifacts